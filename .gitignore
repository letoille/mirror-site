# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# 构建用的依赖（产物 HTML 是提交的，服务器不需要 node）
node_modules/

# 截图调试用的临时副本
_dbg_*.html

# 凭据。**这个仓库是公开的**（下载页链的就是它的 Releases），
# 百度推送 token 写进来等于发布出去，谁都能拿它把每日配额刷空。
.env.local
