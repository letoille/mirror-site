{
  "name": "mirror-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs && node check.mjs",
    "dev": "node build.mjs --watch",
    "serve": "node build.mjs && python3 -m http.server 8765 --bind 127.0.0.1",
    "check": "node check.mjs"
  },
  "dependencies": {
    "markdown-it": "^14.1.0"
  }
}
